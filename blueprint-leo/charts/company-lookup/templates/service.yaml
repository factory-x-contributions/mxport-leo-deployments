apiVersion: v1
kind: Service
metadata:
  name: {{ include "company-lookup.fullname" . }}
  labels:
    {{- include "company-lookup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.server.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "company-lookup.selectorLabels" . | nindent 4 }}
