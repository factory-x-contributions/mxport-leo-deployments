apiVersion: v1
kind: Service
metadata:
  name: {{ include "sts-chart.fullname" . }}
  labels:
    {{- include "sts-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sts-chart.selectorLabels" . | nindent 4 }}
